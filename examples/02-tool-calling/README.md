# 02 — tool-calling

Multi-tool selection with `ToolContext` for state reads and writes.
An invoice assistant with three tools: lookup, mark-paid (gated on
admin), and list-recent.

Covers: Chapter 4 (tools).

## Run

```bash
pip install -r requirements.txt
python -m agent
```
