# 09 — computer-use-browser

A browser-operating agent using the Gemini computer-use preview
model and Playwright. Includes a domain allowlist and an approval
callback for destructive clicks.

Covers: Chapter 7.

## Setup

```bash
pip install -r requirements.txt
playwright install-deps chromium
playwright install chromium
```
