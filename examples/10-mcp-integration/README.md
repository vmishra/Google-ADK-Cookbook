# 10 — mcp-integration

Connects an agent to a Notion MCP server via stdio. The agent can
search and create Notion pages.

Covers: Chapter 4 (MCP tools).

## Setup

```bash
pip install -r requirements.txt
export NOTION_TOKEN=secret_...
```
