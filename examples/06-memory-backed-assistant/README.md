# 06 — memory-backed-assistant

Assistant that remembers user preferences across sessions using
`VertexAiMemoryBankService` (or `InMemoryMemoryService` for local
runs).

Covers: Chapter 10 (memory).

## Setup

For local runs, nothing extra. For Vertex:

```bash
export AGENT_ENGINE_ID=your-engine-id
```
