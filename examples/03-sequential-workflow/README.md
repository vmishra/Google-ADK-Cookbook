# 03 — sequential-workflow

A planner → researcher → writer pipeline using `SequentialAgent`.
Each step writes to session state; the next step reads from it.

Covers: Chapter 3 (sequential agent).

## Run

```bash
pip install -r requirements.txt
python -m agent
```
